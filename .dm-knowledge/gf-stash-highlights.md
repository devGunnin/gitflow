- **[pitfall]** sync.pull_strategy default regressed to 'merge' in 486be1a (PR #145, a floating-window/keybinding commit) and was cemented by ee2ceae (PR #243) retrofitting scripts/test_stage7.lua to match code rather than intent. Restored to 'rebase' — the value the docs, commands.lua's internal fallback, and the original ad62ea2 introduction all use.  _(2026-07-19T23:19:53Z)_
- **[convention]** config.setup() validates against M.defaults() as the schema: unknown keys error with a dotted path + nearest-key suggestion, and colliding keybinding values error. Adding a new option means adding it to defaults() — otherwise setup() rejects it. 'highlights' is an intentional open map (arbitrary group names) via OPEN_CONFIG_PATHS.  _(2026-07-19T23:19:58Z)_
