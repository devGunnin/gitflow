- **[convention]** Panels guard async callbacks with a request-generation counter: bump an id when state changes (open/refresh/view-switch/close), capture it before the async call, and return early in the callback if it no longer matches. References: panels/prs.lua (view_request_id), panels/actions.lua (request_id, also checks expected view), panels/worktree.lua (enrich_gen), panels/diffview.lua (open_request_id). close() must bump too, so a late response cannot resurrect a closed panel.  _(2026-07-19T23:23:30Z)_
- **[pitfall]** scripts/test_stage8_signs.lua drives a real git temp repo with fixed vim.wait(250) sleeps, so it fails intermittently under machine load (seen 2/20 while other test loops ran; 12/12 and 8/8 clean when idle). A one-off failure there is a timing artifact, not a regression - re-run it in isolation before investigating.  _(2026-07-19T23:23:30Z)_
