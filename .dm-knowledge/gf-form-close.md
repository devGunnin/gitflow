- **[invariant]** Any ui/ float that owns cleanup (draft save, on_cancel) must wire ui.window.open_float's on_close hook AND latch at the TOP of its cancel path, not just inside close_*(). The hook fires on every close route; a latch only in close_*() still lets the keymap path call on_cancel a second time. From the hook, skip nvim_buf_delete (bufhidden=wipe collects it) and vim.schedule the callback out of the WinClosed autocmd.  _(2026-07-20T14:12:32Z)_
- **[pitfall]** ui.window.open_float returns nil when the terminal is too small for a usable float (MIN 20x3 + border); callers must guard. An unguarded nil winid does not crash - nvim_set_option_value{win=nil} silently targets the CURRENT window - so the failure is an invisible/broken panel, not an error. lua/gitflow/panels/palette.lua:887,902 is still unguarded.  _(2026-07-20T14:12:32Z)_
- **[pitfall]** gitflow form buffers are buftype=acwrite and are always 'modified' once rendered, so plain :q answers E37 rather than closing. The reachable close routes to test are :q!, :bd! and a layout change (nvim_win_close force).  _(2026-07-20T14:12:32Z)_
