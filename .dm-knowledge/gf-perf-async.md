- **[pitfall]** signs.lua and statusline.lua both hook BufWritePost and both run 'git rev-parse --show-toplevel'. To count sign-update git spawns in a test, stub statusline.refresh and count rev-parse: signs' own update_ticks already collapses the tail of the chain, so the later diff/status commands stay at 1 even with no debouncing and cannot reveal duplicate spawns.  _(2026-07-19T23:00:57Z)_
