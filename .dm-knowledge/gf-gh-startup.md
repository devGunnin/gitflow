- **[invariant]** gh prerequisites are checked lazily by gh.ensure_prerequisites() on the first GitHub command, never in gitflow.setup(). `gh auth status` calls the GitHub API (~410ms online, unbounded offline), so it must stay off the startup path. Any test asserting gh.state.checked right after setup() must call gh.ensure_prerequisites() first.  _(2026-07-19T23:20:17Z)_
- **[pitfall]** vim.fn.writefile turns an embedded \n inside a list item into a NUL byte, producing an unexecutable script ('cannot execute binary file'). Write shell fixtures as one list entry per line.  _(2026-07-19T23:20:17Z)_
- **[pitfall]** The E2E suite is intermittently flaky independent of any change: tests/helpers.lua run_suite iterates tests with pairs(), so test order is nondeterministic per process. Measured over 12 runs each on an unmodified base: full_repo_flow_spec 1/12, async_spec 2/12. Re-run before blaming a diff for a single spec failure.  _(2026-07-19T23:25:20Z)_
