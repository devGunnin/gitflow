- **[convention]** ui.window.register(winid, {name, on_close}) is the single owner of window close hooks: it fires on_close exactly once across every close route (WinClosed covers :q, <C-w>c, :bd, layout changes and ui.window.close; VimLeavePre covers quitting nvim). Any panel or view that must persist unwritten work on close wires it there instead of relying on its own q keymap.  _(2026-07-19T23:23:43Z)_
- **[invariant]** ui.window.open_float returns nil (with a user-facing notification) when the terminal cannot hold a usable float; callers must nil-check. A fractional width/height stays a share of the editor, then gets clamped to the tabline/statusline/cmdheight-aware usable area so content+border always fits.  _(2026-07-19T23:23:43Z)_
